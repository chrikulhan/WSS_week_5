//in a condition with and both conditions must be true
//use && for and

//or statements use || (pipe symbols)
//prerequisites for Android programming = C# or Java

let takenCSharp = false
let takenJava = true

if(takenCSharp || takenJava) {
    console.log('You meet the pre-requisites for Android.')
} else {
    console.log('You must take C# or Java')
}

